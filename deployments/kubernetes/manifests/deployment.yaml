---
# Source: reloader/templates/deployment.yaml
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: reloader
    group: com.stakater.platform
    provider: stakater
    version: 0.0.4
    chart: "reloader-0.0.4"
    release: "RELEASE-NAME"
    heritage: "Tiller"
  name: reloader
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: reloader
      group: com.stakater.platform
      provider: stakater
  template:
    metadata:
      labels:
        app: reloader
        group: com.stakater.platform
        provider: stakater
    spec:
      containers:
      - env:
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: "stakater/reloader:0.0.4"
        imagePullPolicy: IfNotPresent
        name: reloader
      serviceAccountName: reloader
              
